# ExcelToPDF
Автоматизация экспорта Excel листов в PDF

Утилита для автоматизации экспорта инвойсов и спецификаций из Excel в PDF. Скрипт использует win32com для управления установленным Excel, что гарантирует сохранение оригинального форматирования.
🚀 Возможности:
- Интерактивное меню: Удобный консольный интерфейс с возможностью повторного запуска без перезагрузки скрипта.
- Пакетная обработка: Сканирует папку и конвертирует множество файлов за один запуск.
- Фильтрация по ID: Обрабатывает файлы только из указанного диапазона номеров (например, 3550-3560, 3570).
- Два режима экспорта:
1. Инвойс + Спецификация (первые 2 листа).
2. Инвойс + Спецификация + Весовой сертификат (поиск по имени листа весового сертификата).
- Умная область печати: Автоматически задает PrintArea, считывая значение из ячейки R1 каждого листа.

🛠 Требования
- Windows OS
- Microsoft Excel (установленный)
- Python 3.x
- Установленная библиотека pywin32 (pip install pywin32):
После скачивания утилиты запустите файл "Install requirements.bat" для установки библиотек

Batch Excel Processor: Скрипт рекурсивно обходит директории в поисках файлов .xlsx с ключевым словом "invoice"
Парсит номера файлов и сверяет их с пользовательским диапазоном
Используя OLE-автоматизацию (Win32 API), открывает книги в фоновом режиме, группирует необходимые листы (включая динамический поиск листов "Weight certificate") и экспортирует их в единый PDF-файл
Реализована обработка ошибок и циклическое меню для непрерывной работы.

https://github.com/user-attachments/assets/9b9c4314-8374-4766-8380-dc4f8daaa099
